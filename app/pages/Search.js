const Search = () => {
    // Fetch categories data

    return (
        <section>
            <h2>Search</h2>
            {/* Render categories data */}
        </section>
    );
};

export default Search;
